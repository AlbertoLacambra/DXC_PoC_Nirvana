apiVersion: v1
kind: Service
metadata:
  name: control-center-ui
  namespace: cloudmind
  labels:
    app: control-center-ui
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: control-center-ui
